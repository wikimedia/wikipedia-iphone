- Expand MapViewController to support a list view. There is a hidden toolbar on its navigation controller with search and location buttons as well as a map/list switch not yet implemented

- WikiViewController needs to add viewed entries to history (simple fix). Also needs a bookmark button and similar features to the main view.

- Make bookmarks reorderable

- Fix Map crash when closed before fully loaded